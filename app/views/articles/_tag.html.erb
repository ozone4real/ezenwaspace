<%# Single tag component: expects local `tag:` %>
<%= link_to articles_path(tag: tag.name), 
    class: "px-2 py-1 bg-purple-100 text-purple-700 text-xs font-medium rounded-full hover:bg-purple-200 transition-colors" do %>
#<%= tag.name %>
<% end %>