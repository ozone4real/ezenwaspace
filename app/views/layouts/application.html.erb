<!DOCTYPE html>
<html>

<head>
  <title><%= content_for(:title) || "EzenwaSpace" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
  <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <%# Includes all stylesheet files in app/assets/stylesheets %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

  <%# Prism.js CSS for code highlighting %>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/prismjs@1.29.0/themes/prism-tomorrow.min.css">

  <%= javascript_importmap_tags %>
</head>

<body data-controller="code-highlight" class="bg-gray-50">
  <!-- Modern Navigation -->
  <nav class="bg-white/80 backdrop-blur-lg border-b border-gray-200/50 fixed w-full top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center py-4">
        <!-- Logo -->
        <div class="flex items-center space-x-8">
          <%= link_to root_path, class: "group flex items-center space-x-3" do %>
          <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-pink-600 rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path>
            </svg>
          </div>
          <span class="text-2xl font-black text-gray-900 group-hover:text-purple-600 transition-colors">
            Ezenwa<span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-600 to-pink-600">Space</span>
          </span>
          <% end %>

          <!-- Desktop Navigation Links -->
          <div class="hidden md:flex items-center space-x-1">
            <%= link_to 'Articles', articles_path, 
                  class: "px-4 py-2 text-gray-600 hover:text-purple-600 hover:bg-purple-50 rounded-xl transition-all duration-300 font-medium" %>
            <%= link_to 'About', about_path, 
                  class: "px-4 py-2 text-gray-600 hover:text-purple-600 hover:bg-purple-50 rounded-xl transition-all duration-300 font-medium" %>
          </div>
        </div>

        <!-- Right Side Actions -->
        <div class="flex items-center space-x-4">
          <% if admin? %>
          <%= link_to new_article_path, class: "group relative px-6 py-3 bg-gradient-to-r from-purple-600 to-pink-600 text-white font-bold rounded-xl shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all duration-300 overflow-hidden" do %>
          <span class="relative z-10 flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            <span class="hidden sm:inline">Write</span>
          </span>
          <div class="absolute inset-0 bg-gradient-to-r from-pink-600 to-purple-600 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          <% end %>
          <% end %>

          <!-- Authentication Links -->
          <% if logged_in? %>
          <span class="hidden md:inline text-sm text-gray-600">Hello, <%= current_user.email.split('@').first.capitalize %></span>
          <%= link_to logout_path, method: :delete, class: "text-gray-600 hover:text-red-600 transition-colors font-medium" do %>
          <span class="hidden sm:inline">Logout</span>
          <svg class="w-5 h-5 sm:hidden inline text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
          </svg>
          <% end %>
          <% else %>
          <%= link_to login_path, class: "text-gray-600 hover:text-purple-600 sm:inline font-medium transition-colors" do %>
          <span class="hidden sm:inline">Login</span>
          <svg class="w-5 h-5 sm:hidden inline text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
          </svg>
          <% end %>
          <% end %>

          <!-- Mobile Menu Button -->
          <button class="md:hidden p-2 rounded-xl hover:bg-gray-100 transition-colors" onclick="toggleMobileMenu()">
            <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div id="mobile-menu" class="md:hidden hidden pb-4">
        <div class="flex flex-col space-y-2">
          <%= link_to 'Articles', articles_path, 
                  class: "px-4 py-3 text-gray-600 hover:text-purple-600 hover:bg-purple-50 rounded-xl transition-all duration-300 font-medium" %>
          <%= link_to 'About', about_path, 
                  class: "px-4 py-3 text-gray-600 hover:text-purple-600 hover:bg-purple-50 rounded-xl transition-all duration-300 font-medium" %>

          <% if logged_in? %>
          <div class="px-4 py-2 text-sm text-gray-500 border-t border-gray-200 mt-2 pt-2">
            Logged in as <%= current_user.email.split('@').first.capitalize %>
          </div>
          <%= link_to logout_path, method: :delete, 
                    class: "px-4 py-3 text-red-600 hover:text-red-700 hover:bg-red-50 rounded-xl transition-all duration-300 font-medium" do %>
          Logout
          <% end %>
          <% else %>
          <%= link_to 'Login', login_path, 
                    class: "px-4 py-3 text-gray-600 hover:text-purple-600 hover:bg-purple-50 rounded-xl transition-all duration-300 font-medium" %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Flash Messages -->
  <% if notice %>
  <div class="fixed top-24 right-6 z-50 max-w-sm">
    <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-xl shadow-lg">
      <div class="flex items-center">
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <%= notice %>
      </div>
    </div>
  </div>
  <% end %>

  <% if alert %>
  <div class="fixed top-24 right-6 z-50 max-w-sm">
    <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-xl shadow-lg">
      <div class="flex items-center">
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <%= alert %>
      </div>
    </div>
  </div>
  <% end %>

  <!-- Main Content -->
  <main class="pt-20">
    <%= yield %>
  </main>

  <!-- Mobile Menu Script -->
  <script>
    function toggleMobileMenu() {
      const menu = document.getElementById('mobile-menu');
      menu.classList.toggle('hidden');
    }
  </script>
</body>

</html>